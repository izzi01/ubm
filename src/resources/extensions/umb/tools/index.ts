// GSD tools are registered by the gsd extension (src/resources/extensions/gsd/).
// This directory previously contained duplicate tool registrations that caused
// extension conflict warnings at startup. Removed in dead-code cleanup.
