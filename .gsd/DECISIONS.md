# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | dependencies | SQLite driver choice | better-sqlite3 with ESM default import | Synchronous API simplifies the GsdDb class — no async/await needed for CRUD operations. ESM default import works with tsconfig moduleResolution: NodeNext and esModuleInterop. @types/better-sqlite3 provides full type coverage. | Yes | agent |
| D002 |  | architecture | Database column naming and mapping strategy | snake_case in SQLite, camelCase in TypeScript, auto-mapped via toSnake/toCamel helpers | SQLite convention is snake_case columns; TypeScript convention is camelCase. Auto-mapping in the GsdDb query layer keeps both idioms clean without requiring consumers to think about column names. | Yes | agent |
| D003 |  | architecture | Model config YAML parsing approach | Simple line-based parser (no YAML dependency) for .umb/models.yaml | The config format is deliberately flat — just `tier:` and `agents:` with key-value pairs. Adding js-yaml or similar would be overkill. A regex/line parser handles this in ~50 lines and avoids dependency bloat. If nested structures are needed later, the loader can be swapped to a real YAML parser without changing the TypeScript interface. | Yes | agent |
| D004 | M105/S01 | packaging | Global install strategy for umb-cli | npm install -g . from the fork repo with GSD_SKIP_RTK_INSTALL=1 and PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 for fast CI installs. postinstall gracefully handles missing network binaries. | The fork already has bin, files whitelist, and postinstall scripts. No architectural changes needed — just validation and edge-case fixes. Skipping heavy downloads (RTK, Playwright) via env vars keeps CI installs fast while allowing users to opt-in to full installs. | Yes | agent |
| D005 |  | architecture | Workspace packages need package.json files in the fork tarball for module resolution | Create package.json for each workspace package with type:"module", main:"src/index.js", and explicit exports maps for subpath imports | The fork directory (dist-test/) ships compiled JS in packages/*/src/ but lacked package.json files. Without these, Node can't resolve @gsd/* bare specifiers. Subpath imports (e.g., @gsd/pi-ai/oauth) require explicit exports entries with .js extensions since Node ESM doesn't auto-append extensions to exports targets. | Yes | agent |
| D006 |  | architecture | Convert native.js from hybrid CJS/ESM to pure ESM | Replace __dirname/require with createRequire(import.meta.url) and import.meta.dirname in native.js | Node 24 rejects files mixing __dirname/require (CJS globals) with import/export (ESM syntax) via ERR_AMBIGUOUS_MODULE_SYNTAX. Since the native package must be ESM (index.js uses import), native.js must also be ESM. createRequire provides the require() function needed for loading native .node addons. | Yes | agent |
